{{- if .Values.rbacHelpers.enable }}
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
    labels:
        app.kubernetes.io/managed-by: {{ .Release.Service }}
        app.kubernetes.io/name: gitea-operator
    name: gitea-operator-gitearepo-admin-role
rules:
    - apiGroups:
        - core.gitea.com
      resources:
        - gitearepoes
      verbs:
        - '*'
    - apiGroups:
        - core.gitea.com
      resources:
        - gitearepoes/status
      verbs:
        - get
{{- end }}
