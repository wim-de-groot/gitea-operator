apiVersion: core.gitea.com/alphav1
kind: GiteaRepo
metadata:
  labels:
    app.kubernetes.io/name: gitea-operator
    app.kubernetes.io/managed-by: kustomize
  name: gitearepo-sample
spec:
  # TODO(user): Add fields here
